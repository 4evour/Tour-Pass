# 已知问题与分支价值记录

更新日期：2026-09-17

基线：2026-09-17 执行 `git fetch origin --prune` 后，`main` 与最新 `origin/main` 均为 `f17a0f22`。本次修复尚未提交或推送。完整实测见 `CODEX_COMPARISON_REPORT.md`。

本文记录本次分支检查发现的待修复问题、验证证据，以及当前本地和远端分支中仍值得保留或迁移的内容。它不包含密钥、令牌或其他敏感配置值。

## 已知问题

### K-001 生产运行时默认值与文档不一致（高优先级）

- 状态：待修复。
- 证据：`trip_agent/runtime.py` 在未设置环境变量时把 Provider 请求上限默认为 18，记忆预算默认为 6 条消息、4000 字符、30 天、40 条地点和 40 条路线证据；`README.md` 与 `trip_agent/.env.example` 描述的目标值分别是 100、4、800、7、24、24。
- 影响：Docker/Render 没有复制根目录 `.env`，生产环境可能实际运行旧的 18 次请求和更大的上下文预算，导致长行程更容易因配额不足或上下文膨胀而表现异常。
- 建议：集中定义默认配置，统一 `runtime.py`、`.env.example`、README 和 `render.yaml`，并增加默认值契约测试。

### K-002 Provider 缓存的零年龄边界存在时序敏感（中优先级）

- 状态：待稳定化。
- 证据：`ProviderCache.get_stale(..., max_age_seconds=0)` 使用 `fetched_at >= cutoff`；对应测试偶发失败，但单独运行 10 次和随后两次完整套件均通过。
- 影响：Windows 时间精度或系统时钟微小抖动可能造成偶发测试失败，也可能让刚写入的过期记录在零年龄边界被误判为可用。
- 建议：注入可控时钟，明确 `max_age_seconds=0` 的严格语义，并用严格边界回归测试覆盖。

### K-003 当前主线缺少 CI 工作流（中优先级）

- 状态：待补齐。
- 证据：当前 `main` 不存在 `.github/workflows/ci.yml`；旧架构分支中曾有 CI，但不能直接复用。
- 影响：配置默认值、测试、编译和 Docker 启动健康状态没有远端自动门禁。
- 建议：为 Python 3.11/3.12 增加 pytest、compileall、配置契约、Docker 构建和 `/health` 启动检查。

### K-004 真实模型和 Provider 链路近期基线覆盖不足（高优先级）

- 状态：已建立广州/重庆真实比较产物，整体稳定性仍未达标。
- 证据：9 月 14 日仅执行本地回归；9 月 17 日新增 8 次原预算 Tour Pass 真实运行，均未交付。临时模型预算 300 秒的重庆实验在 129.911 秒交付，完整度 63、0 段已核验交通。Codex 广州/重庆成功样本分别为 203.010/177.431 秒，也出现过载超时。
- 影响：不能据此确认稳定成功率；天气、铁路、多城市、连续修改和生产链路仍未在此比较中验证。
- 建议：先改善网关稳定性和实体绑定，再以相同模型/推理/工具权限做对照；不要把预算实验或固定骨架实验混入原配置成功率。

### K-005 部分旅行质量指标仍是提示而非硬门禁（中优先级）

- 状态：产品能力未完成。
- 当前缺口：节奏定量阈值、步行强度、预算真实性、主观体验质量、预约库存、实时票价和临时管制。
- 现状：这些内容会进入完整度报告或风险提示，不能覆盖 Hard Validator 的结论。
- 建议：先明确哪些指标需要 Provider 证据，再逐项增加可解释的硬门禁或降级策略。

### K-006 远端仍保留旧 PR 分支（低优先级）

- 状态：待确认后清理。
- 证据：旧 `origin/codex/pr-*` 分支的提交均已进入 `origin/legacy/origin-main-before-trip-agent`，但没有进入当前 Python 主线；`origin/codex/trip-workflow-updates` 和 `origin/feat/agent-planning-optimization` 已进入当前主线但远端引用仍在。
- 影响：远端分支列表噪声较大，容易误判旧架构仍在开发。
- 建议：确认不再需要审计或恢复后，再统一删除远端旧 PR 引用；本次没有执行远端删除。

### K-007 `codex/valuable-fixes` worktree 有未提交改动（操作风险）

- 状态：保留，不清理。
- 位置：`D:\TourPass-valuable-fixes-20260822`。
- 现状：该 worktree 有大量已修改和未跟踪文件，无法仅凭分支提交判断其当前工作价值。
- 建议：先由该 worktree 所属工作确认内容，再决定提交、迁移或丢弃；不要直接删除分支或 worktree。
- 9 月 17 日补充：该分支/目录不再出现在当前注册 worktree 列表；当前仅注册本仓库和 `D:/TourPass-pr-stack-20260822`。上面的内容是历史风险记录，不代表本次可以删除旧目录。

### K-008 自然语言必去地点字段污染（高优先级）

- 状态：已修复并回归；真实新生成的重庆预算实验通过硬校验。
- 复现：`必去李子坝和洪崖洞，10:00到21:00，公共交通为主，同行2位成人，其中一位膝盖不太好，少走路，不吃辣`。
- 原行为：必去数组错误包含时间、交通、人数、膝盖和少走路子句，李子坝/洪崖洞未拆分，时间窗口为空；114.358 秒后返回 MUST_VISIT_MISSING。
- 修复：分离约束子句、识别地点连接词和“到”时间范围，覆盖逗号地点列表和完整重庆需求。

### K-009 SSE 过载处理及重试预算（高优先级）

- 状态：客户端分类/有限重试/共享预算/对冲取消已修复，上游过载仍存在。
- 证据：修复前两次 SSE 过载直接抛 RuntimeError；修复后日志记录 server_error 重试并在同一 120 秒预算内退出。新的成功实验首文本约 62 秒、模型约 125 秒，原阈值会截断这种请求。
- 修复：可重试流式错误使用 LLMStreamError；永久配额错误不重试；Responses 重试不重新获得整段超时；默认关闭对冲，取消时清理主请求；记录 attempts 和 invocation_total_ms。
- 未完成：模型服务可靠性、等待/外层 SLA 的协调、失败后可恢复交互。不能仅增大等待时间宣称过载已修复。
- 指标限制：provider_request_count 不一定累计之前所有重试物理请求；失败用量不完整，不能估计完整账单。

### K-010 真实 POI 候选未绑定，交通证据优势未兑现（高优先级）

- 状态：重庆实测路径已修复并重测；多城市泛化仍需验证。
- 证据：高德返回李子坝单轨穿楼观景平台、洪崖洞民俗风貌区；模型使用描述性名称，严格词面选择没有绑定。最终两次固定骨架实验和一次新生成实验均为完整度 63、0 段已核验交通。
- 影响：必去地点文本保留可以通过硬校验，但没有真实端点和交通证据，不是可直接照着走的核验结果。
- 建议：分离唯一实体、区域建议、活动描述；有类型/地域/歧义约束的别名匹配和有限再检索。禁止取第一条候选或放宽门禁来刷完整度。
- 9 月 17 日续测：加入 Provider 别名、受限描述后缀归一化、用户唯一必去名称检索、类型/区域筛选及同分歧义拒绝。解放碑绑定商圈参考点，不冒充已订酒店。最新模型骨架重新核验后，2 个必去地点绑定成功、4 段交通有真实路线证据，结构完整度 63→74；未放宽校验门禁。

### K-011 行动需求下休息被自动精简（高优先级）

- 状态：休息精简与明确回住宿休息的交通已修复；体力定量门禁仍未完成。
- 证据：首次固定骨架实验删掉“解放碑住宿区休息”。修复后记录膝盖不适、少走路、不吃辣，休息项不再作为普通可选项删去，最终两次实验保留 13:50–15:20 休息。
- 回归：覆盖行动/饮食子句提取，以及轻松节奏精简时保留休息。
- 续测：午休 14:41–16:11，进出住宿区都查到路线；4 段接驳步行合计 2678 米（不含景点内），仍不足以证明满足少走路。缺实际酒店、无障碍入口、累计步行约束与候选交通比较。

### K-012 评测遗漏生产区域解析步骤（中优先级）

- 状态：已修复，旧录制兼容。
- 原因：RecordingAmap/ReplayAmap 没有暴露 resolve_search_city，程序通过 callable 检测而跳过生产区域解析。
- 修复：按底层/录制支持情况暴露并记录区域解析；旧录制保持未执行该步骤的行为。
- 验证：区域调用录制/重放回归通过；最终真实 Provider 实验把重庆解析为 500000 并保存调用记录。较早样本不能标成完全等同生产路径。

### K-013 铁路测试固定日期过期（中优先级）

- 状态：已修复测试，不改真实服务日期规则。
- 复现：系统日期到 9 月 17 日后，两项使用 9 月 14 日出发日的铁路解析用例失败。
- 修复：为用例固定当前时钟到 9 月 12 日，保留原 HTTP、班次和票价解析目标。

### K-014 前端状态与文档夸大完成程度（高优先级）

- 状态：展示错误、README 校验描述、首屏核验范围和手机封面已修复；地图交互仍待完善。
- 修复：free_time 显示“闲”，仅 visit 计入游览点，63 分徽章显示“完整度”而非 READY。README 改为如实区分未绑定、重复、开放冲突、交通缺证据的警告与真正硬失败。
- 验证：实际浏览器 1365×900、390×844 的固定真实结果渲染检查通过，无全页横向溢出。截图保存在 output/playwright；这不是完整 live SSE 端到端测试。
- 续测：显示实际景点定位、已列路线证据、预约待确认；补全活动/交通时刻，地图常规营业时间注明当日待确认；手机徽章不挤占标题，总览可展开。地图仍是坐标列表/外链；未验收完整登录→SSE→生成→分享流程。

### K-015 就近餐饮的交通顺序错误（高优先级）

- 状态：有明确附近地标的场景已修复；餐厅门到门接驳仍未知。
- 复现：排序后“洪崖洞附近晚餐”在先，但住宿区→洪崖洞的交通在晚餐之后才开始；通勤来源还可能被改名成无坐标的午餐。
- 修复：明确“附近/周边/就近”且下一已定位游览点名称或 Provider 别名匹配时，先前往该片区再用餐。餐饮本身保持无 POI/坐标，不伪造餐馆；只在取得真实坐标后同步更新路线起点。必去匹配限定 visit，不再把“李子坝附近午餐”当必去景点。
- 验证：餐前交通顺序、未知餐厅属性及浏览器时间轴回归；最新重庆在 16:11–16:33 前往洪崖洞片区，17:00 晚餐。

### K-016 真实长返程被短估算替换，裁剪后证据端点失效（高优先级）

- 状态：已修复。
- 复现：高德返回洪崖洞→解放碑约 40 分钟公共交通，但原组装逻辑用距离估算替换为约 12 分钟。尾部删活动后可能仍沿用旧端点路线。
- 修复：保留真实耗时/距离；可缩短末项停留但不短于 30 分钟。仍冲突时保留真实路线与结束时间交由校验报错，不删掉末项后复用旧路线，不把结束时间强行截回截止值。统一路线分钟向上取整，与起止时刻一致。
- 验证：40 分钟真实返程、时间预留、不可满足时间窗时保留端点与休息、1519 秒→26 分钟回归。最新重庆 20:10–20:50 返回区域锚点，另留 10 分钟缓冲到 21:00。

### K-017 结构正确尚不等于体验适宜（高优先级，未完成）

- 最新生成仍把低步行写入标题；实际四段接驳约 2.68 公里，李子坝停留 90 分钟，对膝盖不适者未必合适。现有代码是提醒而非步行/久站硬约束。
- 模型概述在程序按时段调整活动顺序后可能失配：原文称晚餐后直接返回，实际时间轴是晚餐→洪崖洞→返回。需在最终组装后同步摘要，并对顺序一致性验收。
- 兜底删除建议仍依据来源粗选，可能包含餐饮或休息；需与实际 optional、必去和行动保护规则统一。
- 缺少具体日期、实际酒店、餐厅、门到门交通、可用电梯/台阶证据，不能将 74 分或 4/4 已列交通证据视作全程可执行保证。

### K-009 等待预算续测说明

- 代码/示例默认改为模型 300 秒、整次运行 420 秒；上限分别为 600/900 秒。用户本地 `.env` 已由 120/180 改为 600/720 秒，需重启服务生效，未修改认证值。
- 新重庆真实生成 212.509 秒，其中模型阶段 208.281 秒。广州第一次 121.153 秒收到 `gateway_concurrency_limit`；加入该临时错误的有限重试后，再试仍在 135.100 秒内连续 3 次上游过载而失败。不是达到 600 秒预算；不应以继续加时替代服务可靠性修复。

## 分支价值评估

下文保留 9 月 14 日检查的历史价值分析；9 月 17 日当前本地分支仅 main、codex/pr-atomic-query-quota 和三个 legacy 归档。旧分支名称不代表现在仍有该本地引用。旧修复思路仍可从远端旧 PR 或 legacy 历史参考，不建议直接合并旧架构。

### 可以直接认为已完成

- `origin/feat/agent-planning-optimization`：已由 `f17a0f22` 合并，包含单次模型骨架生成、事实核验、确定性组装、铁路 Provider、结构化结果展示及回归覆盖。
- `origin/codex/trip-workflow-updates`：已在当前主线祖先历史中，没有额外未合并工作。

### 值得迁移思路，但不建议直接合并

- `codex/fix-reviewed-bugs`：包含显式管理员和配额策略、Agent 会话边界、POI 事务一致性、前端竞态、容器健康检查和 API 契约修复；这些提交针对旧 C++/多 Agent/React 架构，直接 cherry-pick 会带回已移除的代码。应把它当作当前 Python 系统的安全回归清单。
- `origin/codex/pr-agent-session-safety`：会话持久化和资源边界思想仍有价值，但当前系统已有 `TripStore`、所有权隔离和记忆预算，应补对等测试而不是合并旧实现。
- `origin/codex/pr-atomic-query-quota`：原子预占和失败返还配额的思想仍适用于当前访客/账号额度；需要基于现有 SQLAlchemy 存储重新实现并测试，不能合并旧 C++ 代码。
- `origin/codex/pr-docker-agent-health`：健康检查和容器启动失败可观测性仍值得迁移；当前已有 `/health` 和 Render health check，可补 Docker 启动 smoke test。
- `origin/codex/pr-api-contracts`：错误响应稳定性和未知城市契约仍值得对照检查；当前应落实到 Pydantic/FastAPI 契约测试。
- `origin/codex/pr-poi-transactional-updates`：事务一致性原则仍值得检查当前 `TripStore` 的保存路径，但旧 POI 管理实现不属于当前产品范围。
- `origin/codex/pr-frontend-race-fixes`：竞态修复经验可用于当前 SSE、保存和前端状态更新，但旧 React 编辑器已不在主线，不能直接移植。
- `origin/codex/pr-explicit-admin-bootstrap`：显式管理员配置原则仍适用于生产安全检查；当前服务已不再暴露旧 C++ 管理入口，适合转成配置/部署测试。
- `codex/valuable-fixes`：旧分支中有依赖升级和 CI 调整，但针对旧系统；可只参考 CI 结构和依赖更新策略。

### 仅作归档，不建议合并

- `legacy/local-worktree-before-trip-agent`：保存迁移前本地脚本和工作区。
- `legacy/origin-main-before-trip-agent`：保存旧 C++/多 Agent 主线和旧 PR 合并历史。
- `legacy/tour-pass-before-trip-agent`：迁移前的 Tour Pass 基线。

## 本次验证

9 月 17 日修复后：

- `py -3.11 -m pytest -q`：`97 passed, 2 subtests passed`。
- Ruff check、Ruff format --check、compileall、JS 语法、Oxlint、diff --check：通过。
- 浏览器固定真实结果渲染：桌面/手机完整度、休息标签、两个景点统计、单日结构和横向溢出检查通过；唯一已见控制台错误是 favicon 404。
- 已成功 fetch 最新 origin，main 与 origin/main 仍完全一致；修复只在本地工作区，没有提交/推送。

9 月 14 日历史验证：

- `py -3.11 -m pytest -q`：最终两次完整运行均为 `87 passed`；曾出现一次缓存零年龄边界的偶发失败，已记录为 K-002。
- `py -3.11 -m compileall -q trip_agent tests`：通过。
- `git diff --check`：通过。
- 当前 `main` 与 `origin/main`：完全一致。

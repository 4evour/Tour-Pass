# Force LF line endings for shell scripts (Docker requirement)
*.sh text eol=lf
entrypoint.sh text eol=lf

# Keep CRLF for Windows files
*.bat text eol=crlf
# Force LF for Dockerfile
Dockerfile text eol=lf

# Keep generated editor assets deterministic across platforms
web/editor-dist/** text eol=lf

# DeepSeek Flash 多轮评测与可观测性复盘

日期：2026-09-18。模型为 `deepseek-flash`，使用 Chat Completions，关闭思考模式；每批包含长沙、青岛、重庆三个一日请求。原始录制位于 Git 忽略的 `artifacts/trip-agent-eval/`，本报告不复制请求正文、密钥或 Provider 原始响应。

## 多轮结果

| 批次 | 代码状态 | 成功/总数 | 硬校验通过率 | 平均完整度 | 输入/输出 Token | Provider 调用 |
|---|---|---:|---:|---:|---:|---:|
| `20260918T141025Z` | 首批真实 DeepSeek 输出 | 0/3 | 0% | 无 | 2484/4301 | 20 |
| `20260918T141713Z` | 类型别名与必去匹配修复后 | 2/3 | 67% | 68.50 | 2484/3695 | 24 |
| `20260918T142256Z` | 缺失类型、非法 `days` 恢复后 | 3/3 | 100% | 70.33 | 2484/3699 | 28 |
| `20260918T150310Z` | 约束优先级与输出契约提示词优化后 | 3/3 | 100% | 70.33 | 3537/3671 | 22 |
| `20260918T162527Z` | 导游式讲解与行前引导提示词优化后 | 3/3 | 100% | 70.33 | 4209/4330 | 26 |

`20260918T162527Z` 在不改变单模型、单轮和硬校验链路的前提下，增加了资深亲切导游的角色要求：总览先讲节奏和主线，每天提供行前 briefing，景点说明回答“看什么、怎么玩、为什么这样安排”，并禁止编造历史细节、票价、排名和精确数据。三条真实输出均出现了可执行的讲解，例如说明岳麓山适合上午、橘子洲不必全程步行、八大关不用走完、洪崖洞建议亮灯前到且少走楼层；三条 replay 均通过。硬指标与上一批相近，说明这次改动主要提升了用户可读性和行动引导，不是 Provider 事实覆盖能力的改进。

提示词优化批次三条结果耗时约 14.8 秒、22.1 秒和 18.5 秒；三次都只调用一次规划模型。硬约束仍全部通过，但新质量指标显示平均实体绑定率仅 20%、路线证据率 66.67%、开放时间核验率 0%，且有 12 个未绑定行程项。这说明提示词优化改善了输出契约和修复可预测性，但不能替代实体解析和 Provider 证据覆盖。

## 每次 Agent 链路

第一批的主要失败点发生在模型输出契约和确定性修复之间：DeepSeek 使用 `type: attraction`，而系统只接受 `visit`；部分结果缺少 `type`，一次把 `days` 输出成数字。结果是必去地点被判定缺失，或修复阶段直接抛出类型错误。工具网络本身没有报错，失败发生在工具查询之前或校验阶段。

第二批加入类型别名归一化后，长沙和重庆进入完整组装，青岛仍因必去地点识别失败。这个差异说明只修正一个字段别名不足以覆盖模型的变体输出，必去匹配还需要排除通用餐饮和默认休息项。

第三批增加缺失类型推断、非法 `days` 恢复，以及更严格的必去实体匹配后，三条都完成。提示词优化批次增加了硬约束优先级、精确枚举、地点证据边界和行动负荷要求；DeepSeek 没有再出现 `attraction` 类型或非法 `days`，但重庆仍漏掉两个必去点，由确定性修复补回。三条每个 `tool_started` 都有对应 `tool_finished`，没有 Provider 工具错误；新录制的 `call_id` 生命周期完整，事件索引连续且 trace/event id 唯一。新批次三条录制均已用 `evaluate replay` 重放成功，模型与 Provider 请求哈希、调用顺序、录制消费和最终语义哈希全部一致。

## 与 Codex 的横向比较边界

当前 Codex 基线测试明确禁止工具调用，因此 JSONL 能看到 `thread.started`、`turn.started`、`item.completed` 和 `turn.completed`，以及最终回答、耗时和 Token；它不能提供真实工具调用链，也不能暴露模型内部不可见推理。允许工具的 Codex CLI 运行通常会产生 tool call/tool result 事件，但仍只能看到外部事件，不能看到隐藏推理。

Tour Pass 当前比这个无工具 Codex 基线多保留了模型请求指标、Provider 方法与参数摘要、工具响应哈希、校验报告、持久化事件、源代码哈希和可重放录制。两者不是同模型、同提示词、同工具权限的质量或速度实验，不能用这批数据宣布谁整体更好。

## 可观测性现状

已经具备：

- 每次运行有 `trace_id`，事件有连续 `event_id`、`event_index` 和父事件关系。
- 模型、地点、路线、天气和区域解析均有开始/结束事件；结束事件包含耗时、缓存命中、响应哈希或错误。
- 评测记录模型/Provider 请求签名，重放检查请求哈希、调用顺序、完整消费和最终语义哈希。
- 现在的评测汇总还会记录修复次数及原因、工具启动/完成/错误/缓存命中、启动完成缺口和事件链完整率；对应回归测试已加入。
- 现在还会记录实体绑定率、路线证据率、必去覆盖率、开放时间核验率、未绑定行程项和未知步行段；这些指标与 `completeness_score` 分开，避免结构分数掩盖事实覆盖不足。

仍需改进：

- 新代码已为搜索、路线、区域、天气和铁路工具补上跨开始/结束事件的稳定 `call_id`，并在评测中检查未闭合调用；旧批次录制没有回写这些字段，提示词优化批次已经验证该机制。
- Provider 录制保存了完整结果，诊断能力强，但敏感上下文边界依赖 Git 忽略和人工管理；公开报告只能使用哈希、计数和脱敏摘要。
- `completeness_score` 是结构质量指标，不是实体、路线、开放时间或行动负荷的事实保证；前端和报告仍需把这些证据覆盖率独立展示。
- 当前评测请求只有三个城市、单轮、一天行程，尚未覆盖多城换乘、具体日期天气、铁路、儿童/老人、紧预算和连续修改。

## 后续优先级

1. 按 `call_id` 生命周期继续分析并发搜索的墙钟耗时和错误，并把重试、缓存和 Provider 请求 ID 统一关联。
2. 增加实体绑定率、路线证据覆盖率、开放时间核验率和行动负荷未知量，避免只看硬校验和完整度。
3. 固定同模型、同推理强度、同工具条件的 Codex/Tour Pass 对照，并加入多轮和多城市样本；分别报告首字节、模型耗时、工具墙钟耗时和失败原因。
4. 公开或上传评测产物前做字段级脱敏，保留请求/响应哈希和统计，不发布原始上下文或认证信息。

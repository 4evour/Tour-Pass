# Tour Pass 与 Codex 实测版本报告

最新结果见 [延长等待与质量续测报告](TEST_FOLLOWUP_20260917.md)：新增重庆真实生成 212.509 秒，修复后复用输出得到 4 段真实交通；以下保留首轮测试记录，旧的“待修复”状态以续测报告为准。

日期：2026-09-17。范围：当前 Python 主线、真实模型日志、真实高德查询、Codex CLI 输出，以及修复后的结果渲染。

## 1. 结论

当前系统是功能贯通的旅行规划工作台，但还不是稳定、事实充分核验的出行助手。

- 单次骨架生成、确定性组装、保存、分享、逐日展示已经具备。真实模型链路在当前网关条件下仍有明显过载和超时风险。
- Tour Pass 在原等待预算下，修复前 4 次、第一批修复后 4 次均未交付完整结果，不能给出正常生成平均耗时。
- 临时放宽模型等待预算到 300 秒后，重庆新生成案例交付成功，总耗时 **129.911 秒**；约 126 秒在模型，约 4 秒在事实查询、组装、校验和保存。
- Codex 成功样本：广州 **203.010 秒**、重庆重测 **177.431 秒**。另有重庆 300 秒超时和广州最初 180 秒超时尝试，不能忽略这些失败。
- 成功的 Tour Pass 重庆方案完整度 **63/100**，**0 段已核验交通**。虽然通过硬校验，但不能称为事实充分核验的可执行方案。
- 本次已修复需求误解析、流式错误分类与有限重试、重试共享超时、对冲取消清理、行动需求休息保护、评测区域解析记录，以及三个前端状态/统计问题。

最优先改善的不是增加模型调用次数，而是模型服务稳定性、实体绑定、交通证据覆盖和质量状态表达。

## 2. 对比条件与限制

| 项目 | Tour Pass | Codex |
|---|---|---|
| 实际模型 | gpt-5.6-luna | gpt-5.6-sol |
| 推理设置 | low | high |
| 输出方式 | Schema 约束 JSON，程序组装 | 中文 Markdown 直接回答 |
| 实时工具 | 程序调用高德；缺日期时不取天气 | 明确禁止工具、搜索、文件和命令 |
| 模型预算 | 原配置 120 秒；另做 300 秒独立实验 | CLI 外层分别为 180、300、360 秒 |
| 缓存 | 每次独立 Provider 缓存；模型缓存命中单独记录 | 记录 CLI 上报的缓存 Token |
| 使用环境 | 直接运行 TripAgent 初次规划 | CLI 临时空目录、read-only、ephemeral |

Codex CLI 版本：`0.154.0-alpha.6.2`。两边均使用当前配置的兼容服务，不能把这些耗时视作 OpenAI 官方服务 SLA。

**这是当前配置下的产品对比，不是同模型、同推理强度、同系统提示、同工具权限的架构对比。** 两边的用户需求完全一致，但产品系统提示和交付格式不同。Codex 另加了无工具测试约束，实际完成日志中的工具条目为 0。

此次 Tour Pass 测试不是完整 HTTP/鉴权/额度端到端测试，不计浏览器网络与渲染耗时；没有历史会话，因此也不能评价记忆预算或多轮修改质量。两城市测试没有铁路、多城、实时预约、票价或无障碍设施核验。

最初评测包装器缺失区域解析，修复前及第一批重测没有执行该生产步骤。最后两次骨架实验和新的 300 秒预算实验已记录并执行区域解析。每批产物独立保存源代码哈希，不能混成同一版本样本。

官方 Codex 文档访问本次遇到 HTTP 403；相关条件以实际 CLI 帮助、配置安全子集和执行事件为准，不作未验证的官方模型能力或价格推断。

## 3. 测试需求

### 广州三日

第一次去，喜欢老城和本地美食，不要太赶，公共交通为主。日期、抵返时间和酒店未定。要求每日路线、餐饮、住宿区域、交通策略、预算边界和预约事项；不追问，明确假设，不虚构实时信息。

### 重庆一日

住解放碑住宿区，必去李子坝和洪崖洞，10:00 到 21:00，公共交通为主。两位成人，一位膝盖不太好，少走路、不吃辣，未指定日期。要求三餐、通勤、返回住宿、体力风险和预约待办；不追问，不虚构实时信息。

原始完整提示保存在各案例的 `request.json`，可用 `scripts/compare_codex.py` 复跑。

## 4. 全部实时测试结果

| 系统/批次 | 案例 | 耗时/秒 | 交付情况与原因 |
|---|---|---:|---|
| Tour Pass 修复前，第 1 轮 | 广州 | 54.544 | 失败：SSE 服务过载 |
| Tour Pass 修复前，第 1 轮 | 重庆 | 47.615 | 失败：SSE 服务过载 |
| Tour Pass 修复前，第 2 轮 | 广州 | 120.457 | 失败：模型超时 |
| Tour Pass 修复前，第 2 轮 | 重庆 | 114.358 | 模型返回，需求解析污染导致 MUST_VISIT_MISSING 拒绝交付 |
| Codex 最初尝试 | 广州 | 180 秒上限 | 超时，仅进度文字；Windows 清理异常使该次没有规范 summary，作为不完整样本保留 |
| Codex 正式基线 | 广州 | 203.010 | 成功：约 2400 字符可读方案 |
| Codex 正式基线 | 重庆 | 300.643 | 失败：过载断流、重连后仍未交付 |
| Tour Pass 第一批修复后，第 1 轮 | 广州 | 120.800 | 过载重试后共享预算耗尽 |
| Tour Pass 第一批修复后，第 1 轮 | 重庆 | 120.459 | 过载重试后共享预算耗尽 |
| Tour Pass 第一批修复后，第 2 轮 | 广州 | 120.461 | 过载重试后共享预算耗尽 |
| Tour Pass 第一批修复后，第 2 轮 | 重庆 | 120.474 | 约 104 秒才开始正文，120 秒前未输出完成 |
| Codex 重测 | 重庆 | 177.431 | 成功：约 1675 字符可读方案 |
| Tour Pass 最终代码，预算实验 | 重庆 | 129.911 | 成功：临时模型预算 300 秒，完整度 63，0 段已核验交通 |

失败耗时是失败等待时间，不是完整方案生成时间。样本少且负载条件变化，不能估计 P95、稳定成功率或总体速度优势。

300 秒预算实验没有修改 `.env`、生产默认值或生产 180 秒外层预算。它证明 120 秒模型阈值确实会截断至少一类可完成请求，不证明单纯加大等待时间就能解决过载。

### 成功重庆样本耗时拆分

模型日志：连接约 13.020 秒、首流事件约 53.047 秒、首文本约 62.026 秒、模型请求约 125.161 秒。骨架生成后，区域解析、高德搜索、组装、校验、保存约 4 秒。

与 Codex 重庆成功样本相比，本次 Tour Pass 总耗时少约 47.520 秒。但模型和推理设置不同，且 Tour Pass 交通核验未真正形成，不能据此宣称更快且质量更高。

Provider 时长指标是并行调用时长之和，不是串行墙钟时间。例如新重庆样本 Provider 累加时长 11.678 秒，不能把它当成额外串行等待 11.678 秒。

### Token 与成本边界

| 成功样本 | 输入 Token | 缓存输入 | 输出 Token |
|---|---:|---:|---:|
| Tour Pass 重庆预算实验 | 6685 | 5888 | 2190 |
| Codex 重庆重测 | 17046 | 12928 | 1748 |
| Codex 广州 | 17026 | 16128 | 2684 |

Tour Pass 成功实验没有对冲、没有重试，记录为一次物理请求。其他调用的 `provider_request_count` 仅表示成功尝试内部的请求数，不一定覆盖此前重试。失败请求未完整回传用量，且两边模型/网关计价不同，因此本报告不计算金钱成本、累计费用或节省比例。

## 5. 固定骨架实验

使用修复前重庆真实模型输出，保持用户需求不变，跳过新的模型请求，调用真实高德。

| 版本 | 耗时/秒 | 硬校验 | 完整度 | 已核验交通 |
|---|---:|---|---:|---:|
| 修复需求解析，区域步骤尚未补齐 | 4.316 | 通过 | 63 | 0 |
| 最终解析、休息保护和区域记录，第 1 次 | 4.733 | 通过 | 63 | 0 |
| 最终解析、休息保护和区域记录，第 2 次 | 4.623 | 通过 | 63 | 0 |

这不是完整实时生成速度，也不是原请求哈希完全匹配的 ReplayLLM 重放：新上下文已经改变，实验是固定模型输出的后处理消融。保留了原录制文件来源和哈希，不计入实时生成成功率。

最终两次都保留下午休息，并正确得到 `must_visits=[李子坝, 洪崖洞]`、10:00–21:00、两成人、膝盖不适、少走路、不吃辣。说明确定性修复有效；完整度不升说明其余质量问题仍存在。

## 6. 两者具体差距

### 重庆：Codex 的出行建议更具体

Codex 给出了临江门站乘 2 号线直达李子坝、返回解放碑午餐/午休、提前晚餐、洪崖洞上层入口、20:00 开始返程等安排。膝盖需求落实为减少换乘、避免反复下楼、定期坐下休息、允许短途车。

Tour Pass 同样落实两个必去地点、清淡三餐、休息和返程，但交通多为“轨道交通前往”“公共交通接近”，缺线路、站点和入口的具体可操作表达。新模型把洪崖洞选为 half_day，程序展开为约 3 小时；尚无低步行强度阈值证明这种时长适合膝盖不适者。

两边均没有实时无障碍、亮灯或预约证明。Codex 具体线路与入口只是知识建议，不应当作实时核验结果。

### Tour Pass 的数据工具优势没有兑现

真实高德确实返回了“李子坝单轨穿楼观景平台”“洪崖洞民俗风貌区”等候选，但模型写的是“李子坝轻轨站观景区”“洪崖洞景区”等描述性名称，严格词面选择未绑定。

住宿区域查询返回具体民宿，系统没有擅自把住宿区当作某家酒店；餐饮描述查询也无法证明具体餐馆完全不辣。这类拒绝错误绑定是正确的，但需要进一步把“区域建议”和“唯一实体”分开，而不是得到大量无法使用的候选。

后果：没有必要端点坐标，就没有真实交通查询，结果可以校验通过，却仍然有 UNRESOLVED_ENTITY、OPENING_UNVERIFIED、MISSING_ROUTE_EVIDENCE。问题不只是提示词写作，而是模型名称到 Provider 实体的契约和绑定链路。

不能用选第一条候选、模糊放宽验证或伪造坐标来提高分数。应保留类型/地域约束，增加可审计的别名、候选歧义判断和有限再检索。

### 广州：尚缺 Tour Pass 可比成功样本

Codex 的广州方案覆盖三日路线、住宿、餐饮、条件式交通、预算区间和预约确认。清楚声明 3 天 2 晚、中午抵达、傍晚离开的假设，但每天列出的点位仍可能偏多，且地理/营业/预算未实时核验。

Tour Pass 广州没有本次实时成功结果，所以不能判断它的广州方案是否优于或逊于 Codex，也不能拿重庆成功样本替代广州比较。

## 7. 本次已经修复

1. **必去字段污染**：截断时间、交通、同行、行动等约束子句；李子坝和洪崖洞正确拆分；保留逗号分隔真正地点列表。
2. **自然时间窗口**：识别“到”、连字符等连接方式，不再只支持“至”。
3. **流式服务错误**：读取嵌套及顶层错误；已知过载/临时错误有限重试，永久配额错误不盲重试。
4. **共享超时预算**：Responses 的请求与重试不重置超时；退避占用同一预算，耗尽立即停止。
5. **对冲请求**：默认关闭；保留显式配置，修复启动对冲之前取消时主请求泄漏。
6. **行动与饮食约束**：提取明确的膝盖不适、少走路、不吃辣等子句；行动需求下休息项不作为普通可选项被精简。并非完整自然语言理解或步行硬门禁。
7. **评测一致性**：补记/重放区域解析；旧录制仍保留未执行该步骤的行为。
8. **测试日期**：固定铁路用例的当前时钟，避免 9 月 14 日变成过去日期后测试失效，不改真实铁路日期规则。
9. **前端表达**：free_time 正确显示“闲”；休息/返回住宿不计入景点数量；READY 改为“完整度”，不把结构分数当出行保证。
10. **文档事实**：README 不再把未绑定实体、重复 POI、开放冲突和缺路线证据都描述成硬失败。
11. **复用工具**：加入真实产品对比脚本、固定骨架/预算实验参数，以及结果渲染检查脚本。

没有修改 HardValidator 的门禁，也没有为了交付而绕过失败条件。

## 8. 前端下一步

本次使用真实生成结果作为固定数据，实际检查桌面 1365×900 和手机 390×844：完整度标签、休息标签、2 个景点统计、单日结构、无全页横向溢出通过。不是 live SSE/登录/导出的完整浏览器端到端测试；控制台有一个 favicon 404。

截图：`output/playwright/tour-pass-comparison-1365.png`、`output/playwright/tour-pass-comparison-390.png`。

建议按以下顺序改，不先做整套视觉重设计：

1. **状态提前**：首屏独立列出“结构已生成 / 地点核验 X/Y / 路线核验 X/Y / 预约待确认”，低证据覆盖时显示醒目的待核验状态。顶部静态“实时地点·路线·天气”也应改为本次实际可用状态。
2. **手机封面减量**：目前完整度徽章挤窄标题与正文，封面过长；徽章移到独立元数据行，总览默认一两句，保留展开。
3. **当天内容优先**：手机生成后直接定位当天路线，输入区收起为编辑入口；固定按日切换、下一站、导航和返住宿动作。
4. **避免重复**：封面、玩法介绍、当天摘要和手册重复表达；保留概览与逐日操作两个层级，技术证据继续折叠。
5. **真正地图**：当前是坐标列表和外链，不是可查看空间关系的地图。增加按日着色、真实端点连线和“估算/核验”区分，不绘制虚假精确路径。
6. **休息与行动负荷**：休息独立样式，显示步行/换乘/台阶风险和未知状态；能有数据后再提供累计步行分钟，不凭名称估计护膝程度。
7. **失败可恢复**：连接、首文本、生成、核验阶段分开；提供保留需求并重试、改变条件和查看失败原因，避免过载时只建议缩短行程。

## 9. 优化优先级与验收

| 优先级 | 事项 | 应有验收证据 |
|---|---|---|
| P0 | 模型服务与等待预算 | 同网关同模型重复测试；分首文本/输出/过载统计；兼顾外层 SLA，不能只把超时调大 |
| P0 | POI 身份/区域契约 | 固定候选回归：站点、平台、公园、同名酒店不误绑；区域不冒充酒店；重庆必去坐标可追溯 |
| P0 | 质量状态透明 | 63 分、0 路线不得显示 ready；明确区分草案与已核验方案 |
| P1 | 交通、休息闭环 | 中途返回住宿休息产生实际端点与交通；最终返程和行动限制有证据或明确未知 |
| P1 | 生产默认值一致 | runtime、示例配置、README 与部署契约测试一致；见 K-001 |
| P1 | CI 与稳定测试 | pytest/Ruff/编译/部署健康检查；缓存零年龄边界可复现；见 K-002/K-003 |
| P1 | 评测范围扩展 | 广州成功对照、多城、到离时间、儿童/老人、紧预算、恶劣天气、连续修改 |
| P2 | 手机导航、地图、导出 | 固定真实样本的桌面/手机截图、触控流程和长图/PDF 验收 |

正式比较架构时，应再做相同模型、相同推理强度、相同工具条件的对照，并增加多城市、多轮和盲评。当前没有足够证据宣布 Tour Pass 整体优于 Codex。

## 10. 分支与版本状态

2026-09-17 已成功执行 `git fetch origin --prune`。`main` 和最新 `origin/main` 均为 `f17a0f22`，本次修复尚未提交或推送。

现有本地分支为 main、codex/pr-atomic-query-quota 和三个 legacy 归档；注册 worktree 为本仓库与 `D:/TourPass-pr-stack-20260822`。未删除任何本次新发现的分支或 worktree。

- agent-planning-optimization 与 trip-workflow-updates 已进入主线，没有额外内容需要直接合并。
- 旧 PR 分支保存在旧架构归档历史；会话边界、原子额度预占/返还、API 错误稳定性、事务、前端竞态、容器健康检查仍值得转化为当前 Python 系统的测试/实现任务，但不能直接合并旧 C++/React 代码。
- 当前最值得进入 main 的是本次经过验证的 Python/SSE/评测与展示修复，不是恢复旧架构。

保留了用户原有 `.gitattributes` 修改和 `.playwright-cli/` 内容。详细历史分支记录见 `KNOWN_ISSUES.md`，其中未再注册的旧工作树仅作历史风险记录。

## 11. 产物与验证边界

原始证据目录：`artifacts/codex-comparison-20260917/`。

| 目录 | 内容 |
|---|---|
| baseline/tour-pass | 4 次修复前请求、完整录制与失败 |
| baseline/codex | 最初 180 秒尝试的不完整日志 |
| cli-baseline/codex | Codex 广州成功、重庆过载超时 |
| cli-retest/codex | Codex 重庆成功重测 |
| after-fixes/tour-pass | 4 次第一批修复后真实重测 |
| skeleton-ablation/tour-pass | 首次固定骨架实验 |
| skeleton-ablation-final/tour-pass | 最终代码两次固定骨架与真实 Provider |
| timeout-ablation/tour-pass | 最终代码 300 秒模型预算、新生成重庆成功样本 |

每次规范测试保存请求、配置安全子集、录制、事件、结果或错误、summary；Codex 保存 JSONL、stderr 和最终 Markdown。原始上下文与录制不应公开上传；artifacts 已被 Git 忽略。

最终 Python 回归为 **97 passed, 2 subtests passed**。Ruff、格式检查、compileall、JS 语法、Oxlint 和两视口浏览器结果渲染检查通过。

未执行 Docker/Render 部署、生产负载、真实预约/铁路、多轮记忆、完整 HTTP/SSE 成功规划及导出验收。本地浏览器预览使用隔离的测试数据库，不连接用户原运行库；模型配置未加载，不能当作真实生成服务。
# 续测入口（2026-09-17）

本报告保留首轮证据。最新长预算测试、修复后的四段真实交通和可直接查看的重庆行程见 [延长等待与质量续测报告](TEST_FOLLOWUP_20260917.md)。最新真实重庆生成为 212.509 秒，复用模型输出的最终地图重组为 6.329 秒，两者不可混用；最新 Python 回归为 109 项通过。

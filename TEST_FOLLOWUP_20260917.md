# Tour Pass 延长等待与行程质量续测

2026-09-17。本报告接续 `CODEX_COMPARISON_REPORT.md`，以本次保留下来的真实模型输出、地图响应和实际结果页为依据。修改未提交、未推送。

## 先看成品

- [重庆一日行程：可展开、可查看地图的独立页面](output/chongqing-itinerary-20260917.html)
- [重庆完整当天时间轴截图](output/playwright/chongqing-day-detail.png)
- [390px 手机首屏](output/playwright/tour-pass-comparison-390.png)
- [Codex 同提示词的原始回答](artifacts/codex-comparison-20260917/cli-retest/codex/round-1/chongqing-required-stops/answer.md)

页面保留真实生成内容，也保留尚未解决的缺点；不是人工另写一份更漂亮的行程冒充产品结果。页面开头明确说明住宿区域参考点、餐厅/日期缺失和行动风险。

目前产品能交付保存、展示、带地图证据的结构化方案；本次重庆的两个必去地点、三餐、午休和四段交通已串起来。但它仍是需要行前确认的规划辅助，尚不能保证“少走路”或无障碍可执行性。

## 这次实际等了多久

真实生成使用模型预算 600 秒、评测外层 720 秒，并按顺序执行，避免自行制造同账号并发压力。

| 运行 | 结果 | 实际耗时 | 说明 |
|---|---|---:|---|
| 广州三日，首次长预算 | 失败 | 121.153 秒 | 网关明确返回账号并发限制 |
| 重庆一日，新模型输出 | 成功 | 212.509 秒 | 模型阶段 208.281 秒；有过载重试 |
| 广州三日，补入并发错误重试后再试 | 失败 | 135.100 秒 | 3 次上游过载；并非触发 600 秒超时 |
| 重庆，复用上述模型输出并重新查地图、组装 | 成功 | 6.329 秒 | 最后修复状态；不含新模型生成，不可当总生成耗时 |
| Codex 重庆同提示词，上一轮成功样本 | 成功 | 177.431 秒 | 本轮重新阅读日志和回答，没有重复请求 |

本次重庆完整生成比该 Codex 成功样本慢约 35.1 秒（19.8%）。上一轮 Tour Pass 也有 129.911 秒成功样本，说明上游波动很大。样本小、执行时段不同，而且 Tour Pass 为 `gpt-5.6-luna / low`，Codex 为 `gpt-5.6-sol / high`、禁用工具；这不是同模型架构性能实验，也不能据此宣称稳定快慢或全产品成功率。

本次新重庆成功样本的输入为 6685 Token，其中缓存输入 5888、输出 2174；模型阶段约占端到端 98%。并行 Provider 累计耗时不能直接相加为墙钟时间。失败调用用量不完整，不估算总账单。

## 修复前后，行程究竟变好了什么

| 项目 | 新模型输出在较早代码下 | 同份输出经最终代码重新核验 |
|---|---|---|
| 李子坝、洪崖洞 | 描述性名称无法绑定 | 正确绑定观景平台、民俗风貌区 |
| 解放碑住宿区 | 区域候选有歧义，路线起点不可用 | 绑定商圈参考点，明确实际酒店未确定 |
| 已核验路线 | 0 段 | 4 段，带来源与端点 |
| 午休 | 有文本安排，交通缺失 | 14:41–16:11，并计入往返交通 |
| 真实 40 分钟返程 | 旧规则可能改成约 12 分钟估算 | 保留地图 40 分钟，20:50 到区域锚点 |
| 附近晚餐 | 可能先吃饭、后到所在片区 | 先到洪崖洞片区，17:00 用餐 |
| 完整度 | 63 | 74，仅结构分数 |
| 前端 | 模糊上午/下午，分数抢占手机标题 | 活动/交通时刻、首屏证据数量、待确认范围 |

地点匹配使用 Provider 别名、有限描述后缀归一化和唯一必去名检索；排除同名酒店、车站、商铺等不合类型的结果，同分不同实体保持未解决。没有“取第一条结果”、没有把商圈当酒店、没有放宽 HardValidator 来提高分数。

住宿区午休使用住宿区域坐标并保留休息名称。餐厅没有真实实体时依旧是区域建议，不把“不辣”当地图已验证事实。晚餐提前前往片区的判断只适用于名称明确指向下一已定位景点的情况。

## 修复版实际时间轴

请求：重庆 1 天，2 位成人，其中一位膝盖不好；住解放碑，必去李子坝、洪崖洞；10:00–21:00，公共交通为主、不吃辣、无指定日期。

| 时段 | 安排 | 已知与未知 |
|---|---|---|
| 10:00–10:45 | 解放碑住宿区早餐 | 粥、蒸蛋等清淡建议；餐厅未定 |
| 10:45–11:11 | 前往李子坝观景平台 | 临江门→李子坝，轨道 2 号线；接驳步行约 573 米 |
| 11:11–11:21 | 交通缓冲 | 非预约/固定班次 |
| 11:21–12:51 | 李子坝观景 | 实体已定位；90 分钟对本用户可能偏长 |
| 12:51–14:06 | 李子坝附近清淡午餐 | 未指定真实餐馆，餐馆门口接驳未知 |
| 14:06–14:31 | 返回解放碑住宿区 | 2 号线；接驳步行约 573 米 |
| 14:31–14:41 | 交通缓冲 | 住宿为区域锚点，并非酒店门口 |
| 14:41–16:11 | 回住宿休息 | 保留 90 分钟 |
| 16:11–16:33 | 前往洪崖洞片区 | 本次地图方案为 466 路；接驳步行约 750 米 |
| 16:33–17:00 | 缓冲/机动 | 餐厅位置尚未确定 |
| 17:00–18:15 | 洪崖洞附近晚餐 | 清淡、不辣要求需向餐厅确认 |
| 18:15–18:35 | 区域内接驳预留 | 未查询餐厅门到门路线 |
| 18:35–20:10 | 洪崖洞观景 | 主张外部观景；实际入口、灯光、客流未知 |
| 20:10–20:50 | 返回解放碑住宿区 | 本次路线为 181 路接 2 号线；接驳约 782 米 |
| 20:50–21:00 | 最后缓冲 | 不能承诺未确定酒店的门到门抵达 |

四段地图交通共约 113 分钟，接驳步行合计 **2678 米**，不含景区内和未确定餐厅/酒店的额外步行。地图显示的“24 小时营业”等为普通资料，并不是出行当日开放确认；结果页已加限定。

## 与 Codex 的内容差距

| 维度 | 当前 Tour Pass | 对照 Codex 回答 | 判断 |
|---|---|---|---|
| 必去与三餐 | 两必去、三餐、午休和返程都有 | 同样覆盖 | 基础内容已达到可读方案水平 |
| 交通证据 | 四段真实地图结果、坐标与步行量 | 交通时间为条件式估算，无查询证据 | Tour Pass 已兑现部分核验优势 |
| 膝盖不适的处理 | 李子坝 90 分钟；仍以约 2.68 公里接驳的公交方案为主 | 李子坝 40 分钟，中午较长休息；在住宿区吃午晚餐，短途车备选更明确 | 本样本 Codex 的具体取舍更贴合用户 |
| 方案与摘要一致性 | 程序排序后，摘要仍称“晚餐后直接返回” | 叙述与表格总体一致 | 需要组装后同步文案，不能仅相信结构校验 |
| 餐饮 | 保留清淡建议，无真实餐厅 | 同样主要给品类，但点单措辞更集中明确 | 两者都不能证明餐厅真实满足忌口 |
| 阅读与复用 | 时间轴、证据、地图外链、保存版本、手机页面 | 精简 Markdown 表格，首读更快 | Tour Pass 更适合持续使用，仍有重复内容 |

目前最需要优化的是把行动约束转成实际路线选择和停留时长，再用最终时间轴反向生成摘要。更多地图调用或更高结构分数本身不会自动改善护膝体验。

## 仍未完成，按优先级

1. **服务可靠性**：上游过载、账号并发容量；有限重试和长等待已落地，但广州尚无 Tour Pass 成功成品，不能做广州质量胜负判断。
2. **行动负荷**：把累计步行、换乘、久站、休息作为可检查约束；在公交/打车方案间比较，不因用户“公共交通为主”就忽略膝盖限制。车程和费用需实际查询或明确未知。
3. **最终语义一致性**：根据最终时间轴更新概述、日摘要、晚点删减建议；当前模型文案可能残留旧顺序，兜底建议还可能误列休息/餐饮。
4. **具体酒店和餐厅**：现在用区域参考点，不能承诺真正门到门；完善位置后再补餐厅接驳、酒店出入口和电梯确认。
5. **前端后续**：交互地图、手机生成后收起输入区、摘要去重、按日导航；完整 SSE/登录/分享/导出的端到端验收。本次只验证真实结果渲染和独立 HTML 输出。
6. **扩展评测**：稳定网关下同模型、同推理强度对照；多城、抵离时间、儿童/老人、紧预算、具体日期与多轮修改。当前不能声称产品达到生产级稳定性。

## 配置、验证与证据

代码和 `.env.example` 默认模型 300 秒/整次运行 420 秒，上限 600/900；本机 `.env` 按本次用户要求改为 **600/720**，服务重启后生效。没有修改认证值，没有重启或覆盖用户正在运行的业务数据库。

最后 Python 回归 **109 passed, 2 subtests passed**。新增回归覆盖：别名/类型/歧义、住宿区候选、并发错误重试、600 秒预算、真实换乘与步行提示、休息往返、餐前交通、40 分钟真实返程、时间冲突不删休息/改端点、路线分钟向上取整。

Ruff 静态与格式检查、Python 编译、JavaScript 语法、Oxlint correctness/suspicious 检查通过。检查中发现的复制数组后 `sort()` 写法已改为不原地修改的 `toSorted()`。

浏览器用真实结果固定数据检查桌面 1365px、手机 390px 与窄屏 320px：无全页横向溢出、2 个游览点、休息标签、活动时刻、晚餐前交通顺序。独立 HTML 嵌入 CSS，可以本地查看；没有把这次预览冒充完整实时生成流程。

原始证据均保留在 Git 忽略的 `artifacts/codex-comparison-20260917/`：

| 目录 | 内容 |
|---|---|
| `extended-live` | 广州并发限制、重庆 212.509 秒新生成的原始输入输出及日志 |
| `extended-guangzhou-retry` | 广州连续三次过载后的失败，含每次重试事件 |
| `new-skeleton-final` | 修正新名称变体后的第一次 4 段真实路线结果 |
| `final-quality-replay` | 晚餐片区交通修复后的重新组装 |
| `reviewed-quality-replay` | 最后代码的 6.329 秒固定骨架 + 新地图查询结果 |
| `cli-retest/codex` | 前轮 Codex 重庆成功回答及 JSONL 日志 |

中间结果未删除；不重复付费生成来验证纯程序修复。完整原始记录可能包含用户上下文，不应公开上传。代码、报告、截图和本地配置均未提交或推送；保留用户原有 `.gitattributes` 与 `.playwright-cli/` 内容。
